apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-pvc
  namespace: nextcloud
  labels:
    app.kubernetes.io/name: nextcloud
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: csi-rbd-nextcloud
  resources:
    requests:
      storage: 100Gi
